# Distributor Guide contributing guide

To format the files, run:

in Unix shells (eg. Bash, Zsh):

```bash
npx prettier@2.4.1 --write **/*.md
```

in PowerShell:

```bash
npx prettier@2.4.1 --write **/*.md
```
